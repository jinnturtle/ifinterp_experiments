@startuml

start;

:expand aliases;
:parse "things";
while (n < things.size(); ++n)
        :compare name;
        note right: start from longest
        if (exact match) then
                :save match {pos, ref};
        endif
endwhile
:sort found things by pos;
:find action;
:apply action;
end;

@enduml
